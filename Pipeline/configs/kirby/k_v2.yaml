# k_v2: Recurrent PPO (LSTM) baseline for Kirby
extends: base.yaml

paths:
  session_root: "experiments/kirby/k_v2"

env:
  module: "kirby_gym_env"
  class: "KirbyGymEnv"

model:
  type: "RecurrentPPO"
  policy: "CnnLstmPolicy"
  n_steps: 2048
  batch_size: 1024
  n_epochs: 4
